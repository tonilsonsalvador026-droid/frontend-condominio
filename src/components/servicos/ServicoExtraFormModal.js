import React, { useEffect, useState } from "react";import {  X,  Save,  ClipboardList,} from "lucide-react";import { toast } from "sonner";const ServicoExtraFormModal = ({  open,  onClose,  onSubmit,  servicoEditando,}) => {  const [loading, setLoading] = useState(false);  const [formData, setFormData] = useState({    nome: "",    descricao: "",    valor: "",  });  useEffect(() => {    if (servicoEditando) {      setFormData({        nome: servicoEditando.nome || "",        descricao: servicoEditando.descricao || "",        valor: servicoEditando.valor || "",      });    } else {      setFormData({        nome: "",        descricao: "",        valor: "",      });    }  }, [servicoEditando]);  if (!open) return null;  const handleSubmit = async (e) => {    e.preventDefault();    if (!formData.nome || !formData.valor) {      toast.error("Preencha os campos obrigatórios.");      return;    }    try {      setLoading(true);      await onSubmit({        ...formData,        valor: Number(formData.valor),      });      toast.success(        servicoEditando          ? "Serviço atualizado com sucesso!"          : "Serviço criado com sucesso!"      );      setFormData({        nome: "",        descricao: "",        valor: "",      });      onClose?.();    } catch (error) {      console.error(error);      toast.error("Erro ao salvar serviço.");    } finally {      setLoading(false);    }  };  return (    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-md p-4">      <div className="w-full max-w-4xl mx-auto">        <div className="bg-white/40 backdrop-blur-xl rounded-3xl p-8 lg:p-12 border border-slate-200/40 shadow-2xl">          {/* HEADER */}          <div className="flex items-center gap-4 mb-10 pb-8 border-b border-slate-200/30">            <div className="p-4 bg-gradient-to-br from-blue-500/20 to-indigo-500/20 rounded-2xl border border-blue-200/50">              <ClipboardList className="w-8 h-8 text-blue-600" />            </div>            <div>              <h2 className="text-3xl lg:text-4xl font-black bg-gradient-to-r from-slate-900 to-blue-900 bg-clip-text text-transparent">                {servicoEditando ? "Editar Serviço" : "Novo Serviço"}              </h2>              <p className="text-xl text-slate-600 font-semibold mt-1">                Preencha os dados para cadastrar              </p>            </div>          </div>          {/* FORM */}          <form onSubmit={handleSubmit} className="space-y-8">            <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">              {/* NOME */}              <div className="space-y-3">                <label className="font-bold text-lg text-slate-800">                  Nome do Serviço                </label>                <input                  type="text"                  value={formData.nome}                  onChange={(e) =>                    setFormData({                      ...formData,                      nome: e.target.value,                    })                  }                  className="w-full px-6 py-5 bg-white/60 border border-slate-200 rounded-2xl focus:ring-4 focus:ring-blue-200 shadow-xl"                  required                />              </div>              {/* VALOR */}              <div className="space-y-3">                <label className="font-bold text-lg text-slate-800">                  Valor                </label>                <input                  type="number"                  step="0.01"                  value={formData.valor}                  onChange={(e) =>                    setFormData({                      ...formData,                      valor: e.target.value,                    })                  }                  className="w-full px-6 py-5 bg-white/60 border border-slate-200 rounded-2xl focus:ring-4 focus:ring-blue-200 shadow-xl"                  required                />              </div>              {/* DESCRIÇÃO */}              <div className="lg:col-span-2 space-y-3">                <label className="font-bold text-lg text-slate-800">                  Descrição                </label>                <textarea                  rows="5"                  value={formData.descricao}                  onChange={(e) =>                    setFormData({                      ...formData,                      descricao: e.target.value,                    })                  }                  className="w-full px-6 py-5 bg-white/60 border border-slate-200 rounded-2xl focus:ring-4 focus:ring-blue-200 shadow-xl resize-none"                />              </div>            </div>            {/* BOTÕES */}            <div className="flex flex-col sm:flex-row gap-4 pt-8 border-t">              <button                type="button"                onClick={onClose}                className="flex-1 flex items-center justify-center gap-2 px-6 py-4 bg-slate-200 rounded-2xl font-bold hover:bg-slate-300 transition"              >                <X />                Cancelar              </button>              <button                type="submit"                disabled={loading}                className={`flex-1 flex items-center justify-center gap-2 px-6 py-4 rounded-2xl font-bold text-white transition ${                  loading                    ? "bg-gray-400"                    : "bg-gradient-to-r from-blue-600 to-emerald-600 hover:from-blue-700 hover:to-emerald-700"                }`}              >                <Save />                {loading                  ? "Salvando..."                  : servicoEditando                  ? "Atualizar Serviço"                  : "Salvar Serviço"}              </button>            </div>          </form>        </div>      </div>    </div>  );};export default ServicoExtraFormModal;
